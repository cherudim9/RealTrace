#ifndef BASIC_RENDER_H_
#define BASIC_RENDER_H_

#include "basic_geometry.h"
#include <vector>

const int kMaxDepth=8;

class Tracer{

 public:

  int RayTrace(const RayT &r, const std::vector<Renderer*> &objs, PointT &accumulate_color, int now_depth);

};

#endif
